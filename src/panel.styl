@require "config.styl";
@require "util.styl";
@require "card.styl";

.panel {
  margin-bottom: 0.25em;
  @extend .depth-1;
  header, main {
    padding: 0.5em;
  }
  header {
    border-bottom: 1px solid #eee;
    background: #f7f7f7;
  }
}
